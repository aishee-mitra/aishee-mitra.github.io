# Copy to .env and fill in — do NOT commit .env (gitignored)
BLUESKY_BLOG_MODEL="stepfun/step-3.7-flash:free"
# BLUESKY_BLOG_PROVIDER="openrouter"   # optional: route blog compose through a specific provider (unset = Hermes default = Nous Portal)
